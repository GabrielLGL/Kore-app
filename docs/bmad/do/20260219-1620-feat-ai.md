# feat(ai) — Update AIGoal type — bodybuilding/power/renfo/cardio
Date : 2026-02-19 16:20

## Instruction
Mettre à jour le type AIGoal et tous les fichiers backend IA qui en dépendent :
- types.ts : masse/force/perte → bodybuilding/power/renfo
- offlineEngine.ts : SETS_REPS keys, fallback, goalLabels
- providerUtils.ts : GOAL_PROMPT mapping dans buildPrompt

## Classification
Type : feat
Fichiers : mobile/src/services/ai/types.ts, offlineEngine.ts, providerUtils.ts,
           aiService.ts (cascade), __tests__/offlineEngine.test.ts,
           __tests__/providerUtils.test.ts, __tests__/aiService.test.ts,
           __tests__/providers.test.ts

## Ce qui a été fait
1. `types.ts` : `AIGoal = 'bodybuilding' | 'power' | 'renfo' | 'cardio'`
2. `offlineEngine.ts` :
   - SETS_REPS : clés renommées bodybuilding/power/renfo/cardio
   - fallback buildSession : `SETS_REPS.bodybuilding`
   - goalLabels : Bodybuilding/Power/Renfo/Cardio
3. `providerUtils.ts` : ajout `GOAL_PROMPT` dans `buildPrompt`, ligne Objectif enrichie
4. `aiService.ts` : correction du testForm interne (`goal: 'bodybuilding'`)
5. Tests : toutes les fixtures `goal: 'masse'/'force'/'perte'` mises à jour
   vers les nouvelles valeurs + labels attendus mis à jour

## Vérification
- TypeScript : ✅ zéro erreur (npx tsc --noEmit)
- Tests : ✅ 78 passed (services/ai)
- Nouveau test créé : non (tests existants mis à jour)

## Commit
4c66df5 feat(ai): update AIGoal type — bodybuilding/power/renfo/cardio
