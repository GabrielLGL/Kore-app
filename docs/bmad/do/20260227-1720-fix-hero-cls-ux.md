# fix(web) — Hero UX & CLS
Date : 2026-02-27 17:20

## Instruction
`/do docs/bmad/prompts/20260227-1700-audit3-ux-B.md`

## Rapport source
`docs/bmad/prompts/20260227-1700-audit3-ux-B.md`

## Classification
Type : fix
Fichiers modifiés : `web/src/components/sections/HeroSection.tsx`

## Ce qui a été fait
1. **CLS fix** : ajout `minWidth: "11ch"` sur le `<span>` du mot animé — empêche le layout shift quand le texte alterne entre "CORPS." et "POTENTIEL." (longueurs différentes). Transitions opacity/transform inchangées.
2. **Stat label** : `"Pour commencer"` → `"Plan gratuit"` — plus précis, évite de laisser croire que l'app est entièrement gratuite.
3. **Accessibilité** : ajout `aria-label="Présentation de Kore"` sur le `<header>` — les screenreaders annoncent maintenant un label descriptif au lieu du générique "bannière".

## Vérification
- TypeScript : ✅ 0 erreur (`npx tsc --noEmit` depuis `web/`)
- Tests : n/a (composant UI sans logique testable en Jest)
- Nouveau test créé : non

## Documentation mise à jour
Aucune

## Statut
✅ Résolu — 20260227-1720

## Commit
`d6056d5` fix(web): Hero CLS minWidth, stat label, header aria-label
