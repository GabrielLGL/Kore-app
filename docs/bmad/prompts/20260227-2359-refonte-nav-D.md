# Groupe D — StatsCalendarScreen suppression

## Changements

- Imports ajoutés : `AlertDialog`, `useHaptics`, `useCallback`
- State : `pendingDeleteId: string | null`, `haptics`
- `handleConfirmDelete` : soft-delete via `database.write` + `target.update(h => { h.deletedAt = new Date() })` + `setDetail(null)`
- Chaque bloc session : wrappé dans `<View style={styles.sessionBlockHeader}>` (flexDirection row) + bouton trash à côté
- `AlertDialog` rendu dans le ScrollView
- Styles : `sessionBlockHeader` (row, center), `deleteSessionBtn` (padding)

## Comportement

WatermelonDB propage automatiquement la mise à jour observable. La carte détail est reset via `setDetail(null)` après suppression.
