# Groupe B — Bibliothèque + ExerciseHistoryScreen

## ExercisesScreen.tsx

- Rows exercices wrappés dans `TouchableOpacity` → `navigation.navigate('ExerciseHistory', { exerciseId: item.id })`
- Bouton "Supprimer l'exercice" conditionnel : `{selectedExercise?.isCustom && <...>}`
- Navigation typée : `NativeStackNavigationProp<RootStackParamList>`
- Tests mis à jour : 4 tests adaptés pour `isCustom: true`, 1 nouveau test "masque supprimer non-custom", 1 nouveau test "affiche supprimer custom"

## ExerciseHistoryScreen.tsx (NOUVEAU)

- `withObservables` : exercise (findAndObserve), setsForExercise, histories (non-deleted), sessions
- **Carte PR** : poids max × reps + 1RM estimé (`Math.round(weight * (1 + reps / 30))`)
- **Graphique** : LineChart 15 dernières séances (maxWeight par séance)
- **Historique** : liste inversée (plus récent en premier), sans delete

## navigation/index.tsx

- `ExerciseHistory: { exerciseId: string }` ajouté à `RootStackParamList`
- Import et `<Stack.Screen name="ExerciseHistory" ...>` ajoutés
